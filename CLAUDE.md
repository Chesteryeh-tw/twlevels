# twlevels — 台股盤後選股工具

把權證小哥課程的選股邏輯寫成程式，用證交所公開資料每天自動更新。
GitHub Pages 靜態站，沒有後端。https://chesteryeh-tw.github.io/twlevels/

**這份文件是專案的記憶。** 新對話開始時先讀它，不然會重複已經走過的冤枉路，
或是推翻已經用資料驗證過的決定。

> **最後更新：2026-09-22。結論建立在 1337 個交易日（2021-02 ~ 2026-08），涵蓋 2022 空頭年。**
> 讀到這份文件時先確認 `backtest.json` 的 `days`——
> **不是 1337 就代表這份文件的結論過期了，以 `backtest.html` 為準。**
> 注意：**價量類是 1337 天，籌碼類只有 247 天**（籌碼歷史還沒補），兩者不能相提並論。

---

## 使用者

Chester。不是工程師，看得懂邏輯但不寫程式，也不用命令列。
**溝通用中文。** 講結論不要講過程，數字要附上它代表什麼。

他的目標是**實際用這個工具在股市賺錢**，不是做一個看起來厲害的網站。
所以誠實比樂觀重要：測不出效果就說測不出來，我自己做錯就直說。
他會認真讀、會反駁、而且反駁通常是對的——已經有好幾次他的質疑直接改掉了設計。

---

## 檔案結構

| 檔案 | 做什麼 |
|---|---|
| `index.html` | 整個網站。127 KB 單檔，沒有框架。**改它很貴，用局部編輯不要整份重寫** |
| `backtest.html` | 回測報告（由 `btreport.py` 產生，不要手改） |
| `twse.py` | 證交所抓取的共用工具、檔案讀寫 |
| `fetch.py` | 每日行情抓取 |
| `backfill.py` | 歷史回補。**按「日曆天」抓全市場快照**，每天間隔 1.1 秒 |
| `indicators.py` | 均線、布林、斜率、連漲跌、均量比。**前端和回測共用同一支** |
| `chips.py` | 三大法人、融資券、融資維持率、集保 |
| `tdcc.py` | 集保大戶持股 |
| `backtest.py` | 回測引擎 |
| `btreport.py` | 產生 `backtest.html` |
| `finmind_backfill.py` | 走 FinMind API 補每檔的完整歷史日線 |
| `test_chips.py` | 單元測試 |
| `data/` | `daily.txt`、`chips.txt`、`stock/<代號>.txt`、`chip/<日期>.txt`、`stock/_covered.json` |

GitHub Actions：`update.yml`（每日兩次）、`backfill.yml`、`finmind.yml`（都是手動觸發）。
**Actions 大約每兩個月要手動 Run 一次**，否則 GitHub 會停掉排程。

---

## 營運須知（踩過才知道的時間與機制）

### 證交所資料不是一次出齊的

| 資料 | 公布時間 |
|---|---|
| 三大法人 T86 | 16:00 前 |
| **融資融券 MI_MARGN** | **當晚 21:30** |

**所以 `update.yml` 一天排兩次**：台北 16:00（`0 8 * * 1-5`）抓行情、指標、法人；
台北 22:00（`0 14 * * 1-5`）回頭補融資。

22:00 不是 21:30——排在公布的同一分鐘等於跟它搶。

原本只排 16:00 那版**從第一天起就註定拿不到當天的融資**。之前檔案裡有融資的日子，
都是後來補上的，不是當天抓到的。2026-09-21 實測：18:00 是 0 筆、隔天 13:45 是 1981 筆。

`chips.py` 的 `have_day()` **不能只數列數**——三大法人有、融資沒有的「半套檔」列數是滿的。
要另外檢查有融資值的檔數（`MIN_MARGIN_ROWS`）。而且每次跑都要回頭補前 5 天，
否則半套的那天永遠補不回來。

### FinMind 補歷史的跳過判斷

**不能用「本地最舊日期 ≤ 起始日」**。起始日通常給 `2021-01-01`，那天休市，
最舊日期最早只可能是 1/4，條件永遠不成立 → 每次跑都把 1900 檔重抓一遍（跑了兩次、
各浪費四小時才發現）。

現在改成記帳：抓完寫進 `data/stock/_covered.json`，跟歷史一起進 Actions 快取。
**每次跑都要填一樣的起始日 `2021-01-01`**，填更早的會觸發全部重抓。

生效時日誌會寫「已經補過、跳過 1900 檔；實際要抓 0 檔」，跑完約 20 分鐘。
看到「實際要抓 1900 檔」就是壞了，馬上取消。

### 兩個 workflow 不要同時跑

`update.yml` 和 `finmind.yml` 都會 commit `data/daily.txt`。長的那個跑到一半被短的
推一次，收尾 `git pull --rebase` 會衝突，整趟白費。先跑短的，等綠勾再跑長的。

---

## 講義的核心規則

### 三線
```
振幅 A = 高 − 低
壓力 = 收 + 0.75A     交界 = (高 + 低 + 2×收) / 4     支撐 = 收 − 0.75A
```

### 布林
中軌 = MA20，上/下軌 = 中軌 ± 2σ（**母體**標準差，不是樣本）
位階：上軌 = 10、中軌 = 0、下軌 = −10（線性，不夾限）
帶寬 = 上軌/下軌 − 1

### 多空不對稱（講義原文，這點很重要）

**上下軌不是對稱的。** 每一邊只看自己那條線，另一邊是「否決條件」不是反向訊號。

| 原文 | 意思 |
|---|---|
| 「找主投買、**上軌翹**做多，勝率大增」 | 做多看上軌 |
| 「找主投賣、**月線下彎**放空，勝率大增」 | 做空看月線 |
| 「**避開強勢股，強勢股嘎空太恐怖**」 | 做空的否決條件 |
| 「隔日沖主力大買標的可空，但**避開上通斜率超過 3%**」 | 做空的否決條件 |
| 「**做多找翹上軌；做空找位階高**」 | 兩邊的進場點不同 |

### 參數門檻
```
月線斜率 > 1   超級強勢股，若要做空只能短空
月線斜率 < −1  超級弱勢股，若要做多只能短多
上通斜率 > 3   紅燈（做空要避開）   上通斜率範圍大約 ±10
下通斜率 < −3  綠燈
帶寬 < 5   長期振幅小，不適合當沖
帶寬 > 20  短線振幅大，適合當沖
券資比 > 30   高券資比
均量比 > 3 且有隔日沖 → 當天黑K機率大幅提升
```

### 籌碼
```
融資維持率 = 現價 ÷ (估計平均成本 × 融資成數) × 100
             成數：上市 0.6、上櫃 0.5
             損益兩平 166.7%、追繳 130%
整體維持率 = Σ現值 ÷ Σ融資金額
自避 = 自營避險買超 / 成交量   （跟權證有關，隔日沖分點的權證隔天早盤有賣壓）
投信不能當沖，都是波段買 → 第一天大買，隔天早盤有追價效應
融券強制回補：股東會前 2 個月、除權息前。回補日前券資比高 + 主力買 → 容易嘎空
```

---

## 回測方法論（這幾條是血淚換來的，不要改掉）

1. **進場價用「隔天開盤」**，不是訊號當天收盤。
   盤後選股，名單出來時已經收盤了，最快只能明天開盤買。
   用當天收盤會憑空多賺一段跳空，那是騙自己。

2. **「隔天跳空」單獨列一欄**（訊號日收盤 → 隔天開盤）。
   這一段吃不到，但要看得見——有些條件的優勢整個在跳空上，等於不能用。

3. **比較基準是「同一天全市場（成交金額 ≥ 1 億）的平均」**，不是加權指數。
   指數被台積電主導，跟手上那檔無關。

4. **「贏過」要比中位數不是比平均**。平均被飆股拉高，隨便挑一檔本來就有
   一半機率輸給平均。跟中位數比，隨機基準才是乾淨的 50%。

5. **t 值要用「每日平均」算，不是每一筆**。
   同一天選出的股票會一起漲跌（族群性），把每筆當獨立樣本會高估顯著性好幾倍。
   先把每天的超額平均成一個數字，再看這些每日平均穩不穩。

6. **Newey-West 修正持有期重疊**。抱 20 天的話，今天和明天的訊號重疊 19 天。

7. **一定要有隨機對照組**（rnd10 / rnd30 / rnd60）。
   它們的數字就是這套量測方法的雜訊底線。任何情境沒有明顯超過對照組
   ＝ 什麼都沒測出來。rnd10 曾經純靠運氣跑出 +0.98%、t=+2.3。

8. **多重檢定要算機率**。20 情境 × 4 持有期 = 80 格，純運氣會中 3.6 格。
   報結論前先算這個，不然會把雜訊當發現。（2026-09 實際中 20 格，5.6 倍。）

9. **自動標籤的判準只有統計**：t ≥ 2 且超額 > 0，幾個過關取最短的。
   **交易成本刻意不列入判準**——一個條件成不成立，不該取決於誰的手續費折數。
   但標籤旁邊一定要附「淨」＝原始 − 來回成本（當沖 0.235%、隔夜 0.386%），
   **而且 |淨| < 0.15% 要染成灰的**——那種數字滑價就吃掉了。
   淨若 ≤ 0，標籤直接改成「贏大盤但賠錢」，不給正面標籤。

10. **狀態轉移比平均報酬穩定**。選出來的股票 N 天後散落在布林哪一區
    （貼上軌／上半部／下半部／貼下軌），看「掉到中軌以下的比例」——
    那是這筆單走壞的機率，拿來設停損比看平均報酬有用。

---

## 目前測出來的結論（2026-09-21，1337 個交易日）

### 方法論已經驗證過

三組隨機對照全部歸零（20 天 t = +0.6 / −0.5 / +1.1，12 格檢定 0 格顯著）。
多重檢定：正式情境 80 格，|t| ≥ 2 有 **20 格**，純運氣預期 3.6 格 —— **5.6 倍**。
訊號是真的，不是在雜訊裡找圖案。

（上一版 218 天時，「隨機 10 檔」曾跑出 20 天 +0.98%、t=+2.3。現在證實那是運氣。
這就是為什麼對照組不能省。）

### ⚠ 最重要的一課：贏大盤 ≠ 賺錢

**「反彈放空點」20 天超額 +0.56%、t=+4.8（全報告最高），而且六年每年都正。
但原始報酬是 −0.58% —— 股票自己是漲的，空它照樣賠錢。**

它只是「漲得比大盤少」。這種條件**只能當「不要做多」的濾網，不能拿來進場**。
報告裡每個標籤旁邊都附「淨」（原始 − 來回成本），淨是灰的或綠的就不要碰。

### 真的能賺錢的（淨報酬為正）

| 情境 | 標籤 | 10 天超額 | 原始 | 淨 | t | 樣本 |
|---|---|---|---|---|---|---|
| 投信認養股 | 兩週多 | +0.77% | +2.11% | **+1.72%** | +2.1 | 247 天 |
| 投信首日大買 | 兩週多 | +0.62% | +1.93% | **+1.54%** | +2.0 | 247 天 |
| **貼上軌強勢走** | 兩週多 | +0.92% | +1.18% | **+0.79%** | +2.4 | **1337 天** |

**「貼上軌強勢走」是唯一經過長樣本驗證的賺錢條件**，而且純技術面、不用籌碼。
分年看只有 2022 空頭年是負的（−0.31%），其他五年全正 —— 符合它「追強勢」的本質。
投信那兩個只有 247 天，還沒被這次驗證。

### 統計顯著但不賺錢（只能當濾網）

- **反彈放空點**：隔日空、淨 +0.07%（≈ 0）。20 天 t=+4.8 但原始 −0.58%。
- **價背離空（半套）**：隔日空、淨 0.00%。

### 反指標（照它的方向做會穩定賠）

| 情境 | 尺度 | t |
|---|---|---|
| 高週轉高振幅 | 隔日 | **−5.0** |
| 自營避險大買（空） | 兩週 | **−3.4** |
| 月線上升強勢股 | 隔日 | −2.6 |
| 回檔量縮待買 | 兩週 | −2.6 |
| 軋空候選 | 一週 | −2.7 |
| 投信倒貨融資接 | 隔日 | −2.5 |
| 強勢股隔日空 | 兩週 | −2.5 |

**「盤後看到的強勢股，隔天開盤追進去會穩定小賠」在 1337 天上更硬了。**

**「自營避險大買」當空方是 t=−3.4，翻過來就是做多 t=+3.4** ——
跟講義「自營避險買超 → 權證賣壓 → 小殺」相反。但講義講的是十點前的盤中現象，
日線測不到，兩者不一定衝突。

### 被 1337 天推翻的舊結論（218 天時的判斷是錯的）

| 情境 | 218 天 | 1337 天 |
|---|---|---|
| **貼上軌強勢走** | 反指標（隔日）t=−2.4 | **兩週多** t=+2.4。方向相反 |
| 強勢股隔日空 | 隔日空 t=+2.6 | 隔日 t=+1.4 掉出門檻；兩週反指標 t=−2.5 |

「小樣本會編故事」的直接教訓。

### 「往上帶量開布林」——優勢全在跳空，吃不到（2026-09-22 定案）

**這一條走了三個版本才問對問題，不要再走一次。**

原本的條件用「前 10 日最小帶寬」當壓縮判準，但那是**滾動窗口**：布林打開之後，
舊的壓縮值還留在窗口裡整整 10 天，所以突破後連續 10 天條件都成立——
**選到的是「已經開完的」，不是「正在開的」**（使用者拿華經 2468 的圖抓到這件事：
真正發動的 9/11 沒選到，帶寬已經 60% 的 9/18 反而選到）。

修法：加 `bwy`（昨日帶寬 = 今日帶寬 − 今日帶寬變化）要求「昨天還是縮的」。

修完之後門檻掃描（1337 天，全部做多）：

| 昨日帶寬 | 平均檔數 | 1天 t | 5天 t | 10天 t | 20天 t |
|---|---|---|---|---|---|
| 不限（舊版） | 13.7 | −1.9 | −0.3 | −0.5 | −0.7 |
| ≤ 25% | 11.4 | −2.8 | −1.2 | −1.0 | −1.1 |
| ≤ 20% | 10.2 | −3.0 | −1.9 | −1.6 | −1.6 |
| ≤ 15% | 8.4 | −3.1 | −2.5 | −2.4 | −2.2 |
| ≤ 12% | 6.6 | −3.2 | −2.5 | −2.4 | −2.3 |

**是一片往下的高原，不是尖峰**——門檻越嚴、t 越負，四個持有期同向。訊號是真的，
方向是「別買」。現行標籤：**反指標（隔日）**。

**為什麼？看跳空那一欄：+0.34%（原始 +0.69%）。**
盤後看到它剛開布林，隔天確實高開 0.69%——訊號是對的，講義沒錯。但那段吃不到，
從隔天開盤買進去，當天平均再吐回 0.50%。**是我們太晚，不是條件錯。**

反過來做空？原始隔日 −0.50%，當沖毛賺 0.50% 扣成本剩 +0.27%。**不建議**：
t 是算在超額上的、沒有原始報酬的 t；講義明文「避開強勢股，強勢股嘎空太恐怖」；
0.27% 扣滑價就沒了；尾部是連續漲停。

**可用的結論只有一個：「昨天剛帶量開布林的，隔天開盤不要追。」**

注意：四個門檻不是四個獨立證據（同一批資料、巢狀條件），不能拿去算多重檢定的格數。

### 講義規則的驗證狀態

- **「上通斜率 > 3，若要空只能短空」** → 「不能波段空」那半在 1337 天上確認
  （兩週 t=−2.5）；「短空可以」那半掉到 t=+1.4，不成立。
- **「大時區方向不對不要做」** → 218 天時 t=−2.1／−2.2，這次沒有單獨重測拆解組，
  但「月線上升強勢股隔日追進去」的 t=−2.6 是同一件事的側面佐證。
- **「第一天大買，隔天一早有追價買盤」** → 跳空 t=+6.34，但那是 247 天的結果。

## 已知缺陷（報告裡也有列）

1. **籌碼類只有 247 天**，價量類已經是 1337 天。兩者的 t 值不能放在一起比。
   **這是目前最大的問題**，也是待辦第 1 項。
1b. **倖存者偏差**：抓歷史是照「今天還在市場上的 1957 檔」去抓的，
   2021–2025 之間下市、被併購的公司完全不在樣本裡。五年樣本上這個偏誤明顯：
   **做多的數字要打折，做空的數字其實比看到的好一點。**
2. **沒有還原除權息**，除息日被當成下跌。補長歷史後會放大。
3. **沒有產業別** → 不知道選出的 30 檔裡幾檔同族群。30 檔可能只是 1 個部位。
   （t 值已經處理了族群同漲同跌，但實際下單的風險沒有。）
4. **集保只有最近一期**，沒有歷史 → c1、c3 完全無法回測。
5. **券商分點拿不到** → 講義多方四大條件裡的「主力買」一直是缺的。
6. ~~乖離年線只有 9 天可測~~ **已解決**：補完歷史後 p4、p6 有 1128 天。
7. **沒有盤中資料** → 買盤竭盡、漲停打開、隔日沖拉高出貨這類一概測不到。
8. 週轉率用現在的股本回推，中間有增減資會偏掉。
9. 報告顯示的「超額」是每一筆平均，但 t 是用每日平均算的，兩者略有出入。**待修。**

---

## 待辦（優先序）

| | 事情 | 為什麼 |
|---|---|---|
| ~~1~~ | ~~補價量歷史~~ | **已完成 2026-09-21**。走 FinMind API（`finmind_backfill.py` + `.github/workflows/finmind.yml`），1337 天 |
| **1** | **補籌碼歷史** | 籌碼類還是只有 247 天。FinMind 有三大法人／融資融券，照同樣方式再補一次 |
| **2** | **除權息還原** | 1337 天的樣本裡這個偏誤比一年時大得多，會壓低原始報酬 |
| **3** | **標籤搬到主站** | 選股當下就看到「反指標」，而不是事後翻報告。**「往上帶量開布林」現在是反指標，但主站上還是一個沒有警告的多方條件——這是目前最會害人的落差** |
| 4 | 產業別 + 族群集中度 | 部位風險。元大 Spark API 的報價表查詢有產業別 |
| 5 | 篩選條件說明改三行式 | 「找什麼／條件／注意」，講義原文收進展開區 |
| 6 | 上市／上櫃環境標註 | 用各自的等權平均算，不要用加權指數。**標註用，不要拿來關燈**——個股很強、大盤跌，它照樣會漲 |
| ~~7~~ | ~~開布林門檻掃描~~ | **已完成 2026-09-22**，結論見上面那一節。掃描的做法（看高原不看尖峰）留著給其他條件用 |
| 8 | 融券強制回補日曆 | 軋空候選測出來是反指標，可能就是因為沒看回補窗口 |

### 元大 Spark API（申請中）

有：即時行情、最佳五檔、分時明細、分價量表、**K線查詢**、標的資訊（含產業別）、下單、帳務
沒有：**券商分點、三大法人、融資融券**（籌碼一項都沒有）

申請下來要先確認：**K 線能回溯多久**（決定補歷史走哪條路）、**有沒有流量限制**。

架構上它跟公開網站是分開的兩層，不能合併——API 要用他的帳號登入，憑證不能放進
公開 repo 的 Actions，資料也不能公開轉散布。只能做成「本機跑、資料留本機」。

---

## 不要做的事

- **不要碰下單。** Spark API 有下單功能。資料和分析可以做，下單的程式碼不寫也不跑。
- **不要要求或處理任何憑證**——GitHub token、API 金鑰、密碼。使用者曾經貼過明文密碼，
  已請他改掉並開 2FA。這類東西只該貼進程式要它的地方，不該經過對話。
- **不要抓 StockIntelli。** 他們條款明文禁止爬蟲、禁止逆向分析程式邏輯與資料庫結構、
  禁止用其內容建立競爭性產品，站前還有 Cloudflare 驗證。
- **不要逆向元大網站的內部端點。** 「主力進出-分點」那頁的資料是 iframe 載入
  `ytdf.yuanta.com.tw` 的內部服務。沒有公開 API 文件的內部路徑就是內部路徑，
  不會因為換一家公司就變成可以逆向。人工查閱沒問題。
- **不要解驗證碼。** 證交所 BSR 有驗證碼，所以不能批次抓。人工一次查一檔沒問題。
- **BSR 分點資料不要放進公開 repo**（證交所條款不允許轉散布）。
  設計上要存在瀏覽器本機，用匯出/匯入 JSON 搬移。

---

## 工作方式

- **改動累積起來一次推。** 推檔案是最貴的動作（`index.html` 一次約 2 萬 token）。
  跑回測幾乎免費（容器裡的 CPU），算得多不貴，搬檔案才貴。
- 推檔案的流程：寫好的檔案放到使用者的 `下載\twlevels-push\`，
  他拖進 GitHub 的 upload 頁面 commit。**不要叫他用命令列。**
- 改 `index.html` 用局部編輯，不要整份讀進來（整份約 4 萬 token）。
- 做完要檢查、測試。網頁改動要用 Playwright 驗手機（390px）、桌機、深色模式，
  確認 `scrollWidth - clientWidth === 0`。
- 推完要連上線確認真的部署了（GitHub Pages 有一兩分鐘延遲，
  第一次查到舊版是正常的，等一下再查）。

## 網站的設計決定

- 顏色照台股：**紅＝賺、綠＝賠**（跟券商未實現損益一致）。
  但要講明「紅代表照這個方向做有賺」，不是「股價漲」，否則空方會看反。
- 說明要放在表格**上面**，不是底下——手機滑不到。
- 窄螢幕（760px 以下）不要橫向捲十幾欄的表，改成一個情境一張卡。
- 每個數字都要能自己解釋自己。兩個欄位的顏色基準不同（超額看 0、贏過中位數看 50%），
  這件事一定要寫出來，不然同樣是正數卻一紅一綠，看的人只會覺得壞掉了。
